Task: Enhance Deck page branding, color identity highlights, table/graph contrast, and tag lists.

Instructions:
1. Header & Title:
   - Remove the card emoji and the generic "Decks" title at the top of the deck view.
   - Set the page header/title to the Deck Name alongside its commander image/PNG (if available).

2. Deck Details & Taglines:
   - Relocate the short descriptive archetype phrase (e.g., "big mana group slug") so it renders directly beneath the Deck Name rather than under the initial stats table.

3. Color Identity & Visual Accents:
   - Display the official MTG mana symbols representing the deck's color identity.
   - Use the colors from the deck's color identity to add subtle themed highlights/borders/accents across the page so it is no longer flat black and white.

4. Graph & Table Visual Contrast:
   - Audit the styling of tables and graphs across the Decks view.
   - Fix visual blending issues where components melt into the background (especially graphs utilizing dark/black colors).
   - Add distinct border styling, subtle background elevation/card containers, or contrasting fill colors so elements pop clearly from the page background.

5. Game Changers Section:
   - Simplify the Game Changers table/list: remove the "status" column/indicator entirely.
   - Only display the Card Name and its associated Tag.